services:
  - type: web
    name: naijacart-frontend
    env: static
    buildCommand: yarn install && yarn build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
